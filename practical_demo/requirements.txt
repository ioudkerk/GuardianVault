# GuardianVault Practical Demo Requirements

# Core dependencies (from parent package)
# Note: Install the guardianvault package first with: cd .. && poetry install

# HTTP and async networking
aiohttp>=3.9.0
requests>=2.31.0

# WebSocket client
python-socketio[asyncio_client]>=5.10.0

# Command line
argparse

# JSON/YAML
pyyaml>=6.0.1

# Cryptography (if not installed via guardianvault)
ecdsa>=0.18.0

# Bitcoin utilities
base58>=2.1.1
